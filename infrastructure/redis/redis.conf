# Redis local configuration (LOCAL DEVELOPMENT ONLY).
# Redis is cache / locks / rate-limit only — never the system of record.

bind 0.0.0.0
protected-mode no
port 6379
appendonly yes
appendfsync everysec
maxmemory 256mb
maxmemory-policy allkeys-lru
