# Redis local configuration.
# Redis is cache / locks / rate-limit only. Persistence is AOF for local convenience,
# not because Redis is a source of truth.

bind 0.0.0.0
protected-mode no
port 6379
appendonly yes
appendfsync everysec
maxmemory 256mb
maxmemory-policy allkeys-lru
