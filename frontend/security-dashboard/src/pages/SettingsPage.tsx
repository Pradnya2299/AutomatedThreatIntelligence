import { PlaceholderPage } from '@/pages/PlaceholderPage'

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      detail="Admin configuration and user/role management (ADMIN). Local Phase 1 auth is HTTP Basic in-memory users."
    />
  )
}
