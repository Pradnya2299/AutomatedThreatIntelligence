import { PlaceholderPage } from '@/pages/PlaceholderPage'

export function FindingsPage() {
  return (
    <PlaceholderPage
      title="Findings"
      detail="Correlation output: asset × CVE with match explanations from GET /api/findings."
    />
  )
}
