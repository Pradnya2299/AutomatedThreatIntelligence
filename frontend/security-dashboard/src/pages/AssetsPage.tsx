import { PlaceholderPage } from '@/pages/PlaceholderPage'

export function AssetsPage() {
  return (
    <PlaceholderPage
      title="Assets"
      detail="Corporate inventory (hostname, OS, criticality, internet exposure) from GET /api/assets. Demo seed includes 24 hosts."
    />
  )
}
